version: "0.0.11"
name: "Minimal Analysis"
description: "A minimal analysis with one decision, one input, one output."
inputs:
  - id: test_data
    type: data
    description: "Test input"
outputs:
  - id: result
    type: metric
decisions:
  method:
    label: "Method"
    default: option_a
    options:
      option_a:
        label: "Option A"
      option_b:
        label: "Option B"
