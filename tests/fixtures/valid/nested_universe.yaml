id: baseline
description: "Standard pipeline configuration"

decisions:
  - decision_id: cosmology_model
    option_id: flat_lcdm

analyses:
  build_mocks:
    decisions:
      - decision_id: noise_model
        option_id: heteroscedastic
  train_network:
    decisions:
      - decision_id: architecture
        option_id: maf
