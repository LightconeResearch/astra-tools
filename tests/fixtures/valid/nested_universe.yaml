id: baseline
description: "Standard pipeline configuration"

decisions:
  cosmology_model: flat_lcdm

analyses:
  build_mocks:
    decisions:
      noise_model: heteroscedastic
  train_network:
    decisions:
      architecture: maf
