# Default option doesn't exist in options
version: "0.0.11"
name: "Test"
inputs:
  - id: data
    type: data
outputs:
  - id: result
    type: metric
decisions:
  method:
    label: "Method"
    default: nonexistent
    options:
      a:
        label: "A"
      b:
        label: "B"
