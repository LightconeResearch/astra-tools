# Option references non-existent insight
version: "0.0.11"
name: "Test"
inputs:
  - id: data
    type: data
outputs:
  - id: result
    type: metric
decisions:
  method:
    label: "Method"
    options:
      a:
        label: "A"
        insights:
          - nonexistent_insight
