# Invalid decision type
version: "1.0"

analysis:
  name: "Test"
  problem: "Test problem"
  inputs:
    - id: data
      type: data
  outputs:
    - id: result
      type: metric

decisions:
  method:
    label: "Method"
    type: invalid_type  # Should be data, method, or parameter
    options:
      a:
        label: "A"
