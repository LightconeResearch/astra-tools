# Excluded option without excluded_reason
version: "0.0.11"
name: "Test"
inputs:
  - id: data
    type: data
outputs:
  - id: result
    type: metric
decisions:
  method:
    label: "Method"
    options:
      a:
        label: "A"
      b:
        label: "B"
        excluded: true
        # Missing excluded_reason
