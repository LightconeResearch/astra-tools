version: "0.0.10"
name: "Test sub-analysis missing outputs"
description: "Sub-analysis is missing required outputs"
inputs:
  - id: data
    type: data
outputs:
  - id: result
    type: metric

analyses:
  sub:
    inputs:
      - id: sub_data
        from: ../data
    # outputs is missing
