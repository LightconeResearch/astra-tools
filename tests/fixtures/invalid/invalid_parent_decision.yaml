version: "1.0"
name: "Test invalid decision from reference"
description: "Sub-analysis references non-existent parent decision via from:"
inputs:
  - id: data
    type: data
outputs:
  - id: result
    type: metric

analyses:
  sub:
    inputs:
      - id: sub_data
        from: ../data
    outputs:
      - id: sub_result
        type: metric
    decisions:
      bad_ref:
        from: ../nonexistent_decision
