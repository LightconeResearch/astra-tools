# Option has excluded_reason but excluded is not true
version: "0.0.11"
name: "Test"
inputs:
  - id: data
    type: data
outputs:
  - id: result
    type: metric
decisions:
  method:
    label: "Method"
    options:
      a:
        label: "A"
      b:
        label: "B"
        excluded_reason: "This reason is orphaned because excluded is not set"
