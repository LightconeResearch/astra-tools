# Finding references non-existent output artifact in evidence
version: "0.0.11"
name: "Test"
inputs:
  - id: data
    type: data
outputs:
  - id: result
    type: metric
findings:
  bad_finding:
    id: bad_finding
    claim: "Something was discovered"
    created_at: "2024-01-01T00:00:00Z"
    evidence:
      - id: ev1
        artifact: nonexistent_output
        figure:
          type: FigureSelector
          label: "Figure 1"
