# Constraint references non-existent decision/option
version: "0.0.10"
name: "Test"
inputs:
  - id: data
    type: data
outputs:
  - id: result
    type: metric
decisions:
  method:
    label: "Method"
    options:
      a:
        label: "A"
        requires:
          - nonexistent.option
