# Default option is marked as excluded
version: "0.0.11"
name: "Test"
inputs:
  - id: data
    type: data
outputs:
  - id: result
    type: metric
decisions:
  method:
    label: "Method"
    default: b
    options:
      a:
        label: "A"
      b:
        label: "B"
        excluded: true
        excluded_reason: "No longer supported"
