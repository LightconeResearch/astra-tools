[project]
name = "astra"
dynamic = ["version"]
description = "Agentic Schema for Transparent Research Analysis - A declarative specification format for scientific analyses"
readme = "README.md"
license = { text = "Apache-2.0" }
requires-python = ">=3.11"
authors = [
    { name = "EiffL" }
]
keywords = ["science", "analysis", "specification", "workflow", "reproducibility"]
classifiers = [
    "Development Status :: 3 - Alpha",
    "Intended Audience :: Science/Research",
    "License :: OSI Approved :: Apache Software License",
    "Programming Language :: Python :: 3",
    "Programming Language :: Python :: 3.11",
    "Programming Language :: Python :: 3.12",
    "Topic :: Scientific/Engineering",
]

dependencies = [
    "click>=8.0",
    "pyyaml>=6.0",
    "jsonschema>=4.0",
    "pydantic>=2.0",
    "rich>=13.0",
    "pypdf>=4.0",
    "httpx>=0.27",
    "rapidfuzz>=3.0",
]

[project.optional-dependencies]
dev = [
    "pytest>=8.0",
    "pytest-cov",
    "ruff",
    "mypy",
    "types-PyYAML",
    "types-jsonschema",
]

[project.scripts]
astra = "astra.cli:main"

[build-system]
requires = ["hatchling", "hatch-vcs"]
build-backend = "hatchling.build"

[tool.hatch.version]
source = "vcs"

[tool.hatch.build.targets.wheel]
packages = ["src/astra"]

# Automatically bundle spec/draft/ into the package as astra/spec/
[tool.hatch.build.targets.wheel.force-include]
"spec/draft" = "astra/spec"

[tool.hatch.build.targets.sdist]
include = ["src/astra", "spec/draft"]

[tool.ruff]
target-version = "py311"
line-length = 100

[tool.ruff.lint]
select = ["E", "F", "I", "N", "W", "UP"]

[tool.mypy]
python_version = "3.11"
strict = true

[tool.pytest.ini_options]
testpaths = ["tests"]
markers = [
    "network: marks tests as requiring network access",
    "slow: marks tests as slow running",
]
